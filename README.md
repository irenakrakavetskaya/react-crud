# React CRUD (Project Manager)

A small **React + Vite** CRUD-style project manager app.  
Create projects, select a project, add/delete tasks for the selected project, and delete projects (state is managed in-memory via React state).

## Tech Stack

- React
- Vite
- Tailwind CSS

## Getting Started

```bash
npm install
npm run dev
```
